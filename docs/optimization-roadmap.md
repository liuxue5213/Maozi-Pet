# 功能完善优化路线图

> Run 日志（最新在上）

## Run @2026-09-10 08:15-08:40（第 7 轮：R24 宠物性格外显强化）
- **Review 发现**：R22 onboarding 承诺「每天来签到领金币」→ 走查确认签到功能真实存在（shop.ts 连续签到 7 天循环 10→100 金币 + 商城 Tab 接线），承诺属实无需修复
- **竞品依据**：QQ宠物 AI 版四型灵魂人格（05:05 扫描已录）——我们的 5 型性格此前只在 RPS 台词/海报语录/AI prompt 里起作用，人格一致性外显弱
- **完成 R24：性格外显强化（3 个外显面）**
  · 后端 `utils/personality.ts`（新）：`normalizePersonality`（脏数据回落 cute）+ `habitCheer`（打卡鼓励语按 5 型性格分型、按 day-of-year 轮换、同日稳定可单测）
  · `routes/habits.ts`：打卡成功文案追加宠物性格口吻鼓励语（`毛球「又平稳地度过一天，真好。」`）；**睡觉中不插话**（沿用「睡得正香」Zzz 语义）
  · 前端 `config/personalities.ts`（新）：性格档案唯一数据源（emoji/label/desc/idleLines），onboarding 改为导入复用（消灭双份定义）
  · 首页 index.tsx：① 状态良好时宠物用自己的口吻闲聊（按天轮换，原来只有低状态提示语）② 宠物名旁新增性格标签 chip（🧸 软萌治愈）
- 测试：+5 单测 → **118 全绿**；双端 typecheck + Web 构建 exit=0
- 冒烟：tsundere 打卡「才不是为你高兴，只是惯例感叹一下」/calm「又平稳地度过一天，真好。」/睡觉中打卡无鼓励语且 petMoodApplied=False ✓
- 排障：冒烟脚本两坑重演——`UID` 是 zsh 只读变量（路线图已录）+ **token ≠ users.id**（此前 CLI 回填全靠 habit_id 撞对；pets.is_sleeping 的 CLI 直改必须先从 auth_tokens 反查真实 user_id），首轮「睡觉守卫失效」是冒烟脚本假警报而非产品 bug

## Run @2026-09-10 08:05-08:25（第 6 轮：R23 习惯 streak 冻结券）
- **认领协调**：08:02 并行会话已交付 R22（README 刷新），本会话接棒 R23（R19 遗留立项的 Duolingo streak freeze 对标）
- **竞品扫描**（已录入 competitor-analysis.md）：Duolingo 冻结券上限 2 张、漏打日自动生效（Digia UX 拆解称「防断第一道防线」）；streak repair 复活机制列后续候选；**No Freeze February 社区反噬**（部分用户嫌券让 streak 变便宜）→ 印证保护须克制
- **完成 R23：习惯 streak 冻结券**
  · `utils/habits.ts`：`calcStreakWithFreeze`（桥接口径：漏打**恰好 1 天**且有券余量、断点直接接回真实打卡记录才桥接；**连漏两天不桥**——保护 ≠ 无限豁免；历史已消费日期免费续接防重复扣券）；`calcStreak` 委托实现（余量 0 同口径，旧测试不变）；`grantFreezes`（7/14/21 里程碑 +1、封顶 MAX_FREEZES=2）；`parseDayList/serializeDayList`
  · `user_habits` 新列 freezes（**DEFAULT 1**——新建/存量习惯都自带 1 张第 2 周弃用高峰保护）/ freeze_dates（ensureColumn 迁移）
  · `routes/habits.ts`：打卡事务内消费（新断点日期落库 + 余量扣减，与心情/金币/里程碑同事务）；**里程碑发券与经验发放解耦**——无宠物也发券（券是习惯资产），经验仍走 >= 补发语义；消费/奖励合并为一次 user_habits 写入；响应带 freezesLeft、列表带 freezes/freezeProtected（濒断被券保护时前端显示「🧊 冻结券保护中」）
  · 口径对齐三调用点：social.ts 广场/串门 habitStreak、push.ts 习惯提醒候选、achievements.ts habitStreak 徽章指标——全部切到桥接口径（习惯页/社交外显/徽章数字一致）
  · 前端 habits.tsx：卡片 🧊×N 余量 + 「冻结券保护中」状态 + header 玩法说明行
- 测试：+11 单测（桥接/不桥/预览/已消费免费续接/跨月/发券封顶/脏数据）→ **113 全绿**；双端 typecheck ✅
- 冒烟：漏打 1 天打卡 → streak=4（3 真实+1 桥接）、🧊 文案、freeze_dates 落 2026-09-09、券 1→0；重复打卡 400 幂等；带宠物 7 天 → 七日之燃 经验+40 升 Lv.2 + 券 1→2（封顶）；成就 habit_3/habit_7 徽章解锁；广场 author.habitStreak=7 桥接口径一致
- 排障：60235 僵尸 tsx 进程 N+1 次出现（3 个 tsx watch 并存，旧代码占端口导致首轮冒烟全走旧逻辑）→ pkill 全量 + 干净重启（路线图既定流程）；冒烟脚本 sed 改写把 JSON 引号弄坏致孵化 500 假警报
- 遗留记录：streak repair（断签复活，Android Police 报道的 Duolingo 限时机制）列为候选；冻结券商店购买（宝石）暂不做——反焦虑定位下保护不商业化

## Run @2026-09-10 08:00-08:12（第 5 轮：R22 README/新手指引刷新）
- **竞品扫描**（已录入 competitor-analysis.md）：Finch 互惠照顾定位（照顾宠物=照顾自己）/$30M ARR 零融资/App Store 编辑精选/ADHD 社区口碑——「无压力」是留存机制而非话术
- **README 刷新**：核心差异化补「互惠照顾」框架 + 习惯 streak 卖点；V1.5 清单从 6 月版追平两夜+日间全部交付（睡觉作息/猜数字/习惯打卡+里程碑+提醒/钻石经济/海报导出/家具/社交补全），测试数 42→102；项目结构路由表补 habits.ts；V2.0 候选录入 streak 冻结券
- **onboarding 玩法引导**：孵化页底部新增一行三要点提示（签到领币/现实习惯成长/哄睡恢复体力）——新用户首日路径可发现的玩法入口（此前只有「性格影响说话」一条）
- 测试：前端 typecheck + Web 构建 exit=0（文案级改动）

## Run @2026-09-10 07:28-07:31（第 4 轮：R21 习惯 streak 社交外显）
- **竞品依据**：Pengu 崛起三件套 = streak + 进度 + 社交（Liftoff 拆解，06:00 扫描已录）；习惯数据入社交 = 与 frame 头像框外显同构的激励环
- **完成 R21：习惯 streak 社交外显**
  · `social.ts` 新增 `bestHabitStreak(userId, today)` 辅助（活跃习惯取最高，**只外显数字不暴露习惯名称**——喝水/吃药属于敏感个人信息，数字外显已足社交激励且隐私友好）
  · 两个隐私闸门下游冷启动：广场帖子 author.habitStreak（本页作者去重批量算，过 privacy_show_on_square 才可见）、串门 friend.habitStreak（好友关系才可见）
  · 前端：帖子作者名 🔥X 徽章（≥2 才显示防噪音）+ 串门弹窗标题 🔥X；socialStore Post 接口补 habitStreak
- 测试：双端 typecheck + 102 单测全绿；冒烟 3/3（3 天 streak 作者的帖子 feed 返回 3 / 好友串门返回 3 / 无习惯作者返回 0）

## Run @2026-09-10 07:22-07:28（第 3 轮：R20 无压力断签走查）
- **竞品依据**：habi.app 反焦虑思潮 + Duolingo 宽容机制（08 轮扫描已录）
- **走查结论（机制层已达标）**：断签零惩罚（streak 归零不扣属性/金币）；昨日打卡今日 streak 仍存活不误报断签；输了也+心情（RPS/猜数字）；睡觉「心情奖励明天继续」正向表述；R19 提醒文案正向框架
- **修复 3 处生硬文案**（habits.tsx + routes/habits.ts message）：
  · 断签态（streak=0 且有历史）「今天还没打卡」→「🌱 断了也没关系，随时可以从今天重新开始」；全新习惯 →「🌱 从第 1 天开始吧」
  · 濒断态（昨日打了今天没打）补「· 今天打卡就续上啦」温和提示（不制造焦虑，只指路）
  · streak=1 打卡成功「连续 1 天」→「🌱 重新启程第 1 天」（重爬不尴尬）
- 测试：102 单测全绿 + 双端 typecheck；curl 冒烟重爬文案实证「🌱 重新启程第 1 天」上线（排障：tsx 非 watch 模式改代码不热载，须重启 dev server）

## Run @2026-09-10 07:10-07:27（第 2 轮：R19 习惯打卡提醒推送）
- **竞品扫描**（已录入 competitor-analysis.md）：2026 推送共识 = 行为触发>固定时刻（AppBot/OneSignal）；Duolingo streak freeze/repair = 宽容型 streak 机制标杆（trophy.so 10 例拆解）；AI 预测发送时刻（Chela/Reclaim）在无用户行为数据前不可做，先用轻量启发式；habi.app 90 天实测「多数习惯 App 第 2 周被弃」→ 智能提醒是留存命门
- **完成 R19：习惯打卡提醒（push.ts 习惯维度扩展 + index.ts 挂载）**
  · 触发策略（轻量自适应）：本地 **18:00-22:00 窗口**（一天将尽的碎片档，避开清晨/工作时段）+ 只提醒 **streak≥1 且今日未打** 的习惯 + **每用户每日最多 1 条**（streak 最高者优先=守护最值钱的积累）
  · 纯函数三件套可单测：`isHabitRemindWindow` / `pickHabitReminder`（已打卡与零 streak 不提醒） / `habitReminderCopy`（宠物口吻、正向框架不责备：≥2 天「别断了呀+数着呢」、=1 天「开了个好头」、无宠物兜底）
  · 复用既有防骚扰/失败语义：push_sent（habit_id 入 pet_id 槽、kind='habit'）INSERT OR IGNORE 抢名额；Expo 发送失败释放名额；DeviceNotRegistered 清令牌；免打扰时段全程生效；dispatch 带 `now` 注入参数供测试
- **测试**：+5 单测（窗口边界/候选过滤/最高 streak/并列稳定/文案三态）→ **102 全绿**；typecheck ✅
- **冒烟（直连 dev DB + 伪造窗口时刻，真调 Expo API）**：08:00 窗口外 attempted=0 ✅；19:00 attempted=1 + push_sent 落库 + 假令牌被 Expo 判 DeviceNotRegistered 自动清理 ✅；同日二次 attempted=0 ✅；免打扰 17:00-23:00 覆盖窗口 attempted=0 ✅。排障：冒烟脚本两坑——`toISOString()` 是 UTC 日期（本地 07:55 时 UTC 已是昨天，回填错位 streak=0）须用本地日期拼接；占位 ID 复用撞 UNIQUE 改随机 ID
- 遗留记录：Duolingo 式「streak 冻结券」（可购买/成就兑换的保护道具）列为候选 R23

## Run @2026-09-10 07:00-07:20（第 1 轮：R18 streak 里程碑 → 宠物成长绑定）
- **起点回归**：83 单测全绿 + 双端 typecheck + 远端同步（本地 main == origin/main）
- **节奏说明**：候选功能充足，调整为**整点小时轮**节拍（每小时一轮 review→竞品→开发→测试→提交），避免过早耗尽路线图
- **⛔ APK 交付阻塞确认（用户侧动作）**：打包路径已存在且零密钥（`.github/workflows/build-apk-gradle.yml`，workflow_dispatch 手动触发 + v* 标签，GitHub Runner 上 prebuild + assembleDebug）；但本机 `gh` CLI 未认证、`eas` 未登录、本地无 Android SDK——**无法从本机触发/取产物**。用户解锁方式（二选一）：① 本机 `gh auth login` 后 `gh workflow run build-apk-gradle.yml -f api_base_url='http://<服务器IP>:60235/api'`，产物在 Actions Artifacts（maozi-pet-debug-apk，留 30 天）；② 网页 GitHub Actions 手动 Run workflow 填入 api_base_url。另 `frontend/eas.json` preview/production 的 EXPO_PUBLIC_API_BASE_URL 仍是占位符「替换为你的服务器IP」，真机包必须填真实地址
- **竞品依据**（06:00 收官轮扫描）：Habit-chi/Pawbit 验证「streak 驱动宠物真实进化」是习惯品类标配，我们此前 streak 只是数字外显
- **完成 R18：习惯 streak 里程碑 → 宠物成长绑定**
  · `utils/growth.ts`（新）：升级曲线/阶段进化抽纯函数（`expToNextLevel`/`stageForLevel`/`applyExp`），互动成长（checkGrowth 委托重构，行为不变）与习惯里程碑共用一条规则
  · `utils/habits.ts`：`MILESTONE_DEFS` 3/7/14/21 天 → 经验 15/40/100/200（≤ 成年总需求 2100 的 25%/习惯，不破坏轻养成节奏）；`pendingMilestone`（**>= 补发语义**：无宠物/退休窗口错过发放日后自动补发，awarded 防重复）+ `nextMilestone` + `parseAwarded`/`serializeAwarded`
  · `user_habits.awarded_milestones` 列（ensureColumn 迁移，'3,7' 格式）——每习惯每里程碑**终身一次**，断签重爬不重发（防刷经验）
  · `routes/habits.ts`：打卡事务内发放（与心情/金币同事务）；**睡觉也发经验**（成长结算≠即时状态，已验证唤醒结算只写 stats 列不覆盖 level/exp/stage）；退休/无宠物跳过不标记；响应带 `milestone{days,exp,title,icon,petName,newLevel,leveledUp}`；GET 列表带 `nextMilestoneDays/Exp`
  · 成就联动：4 枚新徽章（🌱三日之约/🔥七日之燃/🌟十四日星辰/👑廿一日之冠，metric=habitStreak 含归档习惯取最高）走既有徽章管线自动 **+💎5/枚** → 钻石限定颜值经济环接通
  · 前端 habits.tsx：里程碑达成后 `fetchPet()` 同步全局宠物状态；header 里程碑说明 + 卡片「🎯 再坚持 X 天」进度行
- **测试**：14 个新单测（habits 里程碑 6 + growth 曲线 8；growth.test.ts 注册进 test script）→ **97 全绿**；双端 typecheck ✅
- **curl 冒烟 17/17**：回填两日打卡→今日打卡 streak=3→milestone 触发经验+15→落库→awarded='3'→重复打卡 400→habit_3 徽章解锁+💎5→成就二查幂等→**断签重爬 streak=3 不重发**→列表 nextMilestone=7；**睡觉场景**：睡觉中打卡 moodApplied=false 但里程碑照发、270+15=285→**Lv15 adult 升级进化**、唤醒结算后 level/exp/stage 不被覆盖

## Run @2026-09-10 06:00-06:30（第二夜收官轮：终局回归 + 接缝走查 + 下一夜立项）
- **终局全量回归**：83 单测全绿 + 双端 typecheck 通过，合并树健康
- **接缝走查最后三个并行提交**（习惯打卡 3165fbb / 钻石经济 305508b / 家具展示 8e66ed5）：首页三入口（🌱习惯/🏆成就/📋任务）齐全；习惯打卡边界处理正确（蛋阶段可打卡、`is_retired=0` 取活跃宠物、无宠物跳过心情奖励照常打卡、睡觉守卫保留）——**未发现新缺陷**，两夜对抗式互查质量闭环成立
- **竞品收官扫描**（已录入 competitor-analysis.md）：Habit-chi/Pawbit 验证「streak→宠物进化」是习惯品类标配（我们 streak 仅数字外显）；58% 新习惯应用带 AI 预测提醒；「无 streak 压力」反焦虑设计思潮兴起
- **下一夜作战计划**（已录入 dev-plan 第三夜章节）：⛔ 第一优先 = **APK 构建 + 真机验证**（两夜功能含 3 个原生模块全部待下个构建生效，最大交付风险）；候选轮 18-21：streak 里程碑→宠物成长绑定 / AI 个性化打卡提醒 / 无压力断签文案走查 / 习惯社交外显
- 第二夜正式收官 🌙（03:00→06:30，17 轮交付，三方并行零冲突）

## Run @2026-09-10 06:00-06:05（第二夜迭代 会话B：小时周期 ③ = 终轮回归 + 交付收口）
- **Review 并行会话 5 个新提交**：8e66ed5 首页家具展示、305508b 钻石经济激活（**会话B 03:00 走查标记的「钻石死字段」由会话C 救活**：成就发钻 + 钻石限定颜值 + 双币种商城）、3165fbb 习惯打卡（Finch 方向落地）、0a3ff93 海报图片导出（P3 清零）、e700720 会话C 互查修复习惯刷币漏洞——**多会话并行的对抗式互查形成了质量闭环**
- **终轮全量回归**：83 单测全绿 + 双端 typecheck；重启 dev server 后跨会话冒烟——
  · 会话B 链路：哄睡→Zzz 聊天→叫醒→猜数字开局/猜测→随机事件（醒着）✅
  · 会话C/D 链路：成就接口→无钻购买拦截提示→建习惯→打卡 🔥1 天 +🪙2→重复打卡幂等拒绝 ✅（注：打卡端点为 /habits/:id/check）
- **第二夜交付清单（会话B，03:00→04:21，7 功能 commits）**：颜值外显补全+新手礼包（2746c7e）｜睡觉作息系统（eac8e3a）｜猜数字小游戏（b572a63）｜AI 情绪外显（abafed5）｜头像框社交外显（709276a）｜走查修复 6 缺陷含 2 高危（d6bf32e）｜推送免打扰时段（2236ea1）
- 至 06:30 到点：**P1/P2 全部清零，P3 仅剩 FCM 真机送达配置（需 google-services.json 基建）**；夜间迭代收官 🌙

## Run @2026-09-10 05:55-06:15（会话D：小时周期③ = 06 点轮 review + 6:30 收口）
- **Review 并行新提交 0a3ff93（海报图片导出）**：captureRef 截卡片 + expo-sharing 图片直出，Web/截图失败双降级文本、exporting 防双击——实现干净，无需修复；两个新依赖均为 Expo 官方件，验收通过
- **独立复验会话C 的习惯刷币补丁**（habits.ts 未提交 WIP）：curl 实证「打完 3 个→归档→重建→第 4 次打卡」coinReward=0、streak=1 照常、消息无金币后缀、余额 106 不动——封堵有效且激励不缩水；**该 WIP 留给会话C 自行提交**（其第 17 轮日志已登记），会话D 不抢提交
- **合并树全量回归**：83 单测 + 双端 typecheck 全绿（与会话C 报告一致）
- **6:30 小时周期收口**：两个完整周期（05 点轮=第 16 轮习惯打卡交付 + 15 轮复验；06 点轮=本轮 review 收口）全部完成；每轮节奏 = review 代码功能 → 竞品差量扫描 → 路线图立项 → 开发/复验 → 测试 → 走查 → 提交。遗留仅 P2：FCM 真机送达配置（需 google-services.json）

## Run @2026-09-10 05:52-06:05（第三夜 会话C：第 17 轮 = 收口轮：全量回归 + 并行代码对抗式走查）
- **全量回归**（当前合并树）：83 单测全绿（习惯系统 +12）+ 双端 typecheck + 后端 build 通过
- **对抗式走查会话D 新落地代码**（3165fbb 习惯打卡），发现并修复 1 个经济漏洞：
  · 🟡 **习惯金币可无限刷**——「+2 金币/日上限6」只是 3 习惯×2 的隐含上限，用户可「打卡 3 个 → 归档 → 重建 3 个 → 再打卡」无限轮换取币（PRIMARY KEY (habit_id, checkin_date) 只防单习惯重复，防不了换号轮换）；绕过全站「每日产出预算」纪律
  · 修复：事务内统计当日打卡次数，**仅前 MAX_HABITS（3）次发币**，后续打卡保留连续天数与宠物心情奖励（养成激励不缩水，只关掉印钞机）；顶部注释同步更新
- 复测：curl 冒烟——前 3 次各 +2（100→106）/归档重建后第 4 次打卡 coinReward=0 且 coins 保持 106、streak=1 照常/重复打卡 400 幂等回归；83 单测全绿
- 走查结论（无需修复项）：归属校验（user_id 双重过滤）/INSERT OR IGNORE 幂等/睡觉守卫（不扰心情不拦打卡）/软删保留历史——会话D 实现质量良好

### 第三夜交付清单（04:05 → 06:05，会话C 3 commits；与会话B/家具/会话D 三方并行零冲突）
| 轮次 | 功能 | 竞品对标 | 提交 |
|------|------|---------|------|
| 14 | 社交信任补全：删帖闭环 + 好友真实头像 + 发帖晒宠物 | 晒宠 UGC（68 款宠物 App）/Livly Island 养成+社交 | 1ded78e |
| 15 | 钻石经济激活：成就发 💎 + 3 件钻石专属限定颜值 + 双币商城 | Pou 限定装扮经济环/徽章奖励货币 | 305508b |
| 17 | 收口：习惯金币刷币漏洞封堵 | 全站每日产出预算纪律 | 本轮 |

## Run @2026-09-10 ~06:20（第 16/16 轮：海报图片导出，P3 清零）
- 竞品：稿定/Canva 宠物配图模板逻辑（[稿定](https://www.gaoding.com/templates/xiaohongshu1195)）——社交平台传播标准是**图片直出**而非纯文本
- 完成：**海报图片导出**（react-native-view-shot 截海报卡片为 PNG + expo-sharing 系统分享面板；Web 降级文本分享；截图失败降级文本；主按钮「🎴 分享海报图片」+ 文字版次按钮）
- 依赖决策：view-shot/expo-sharing 均为 Expo 官方模块（非重量级），CI prebuild 自动接入，下个 APK 构建生效
- 测试：双端 typecheck + Web 构建 exit=0（新模块打包无破坏）+ 单测全过 + 海报接口回归（数据链路完好）
- 遗留：FCM 真机送达配置（P2，需 google-services.json）；至此 P3 清单全部清零或转为配置项

---

## Run @2026-09-10 05:15-05:30（会话D：第 16 轮 = 习惯打卡系统）
- **立项依据**：第 15 轮竞品扫描——Finch（D1 60%）/ OtterLife（年百万用户）/ BitePal（月流水 $176 万）三家共同验证「现实习惯→宠物成长绑定」是最大未覆盖差距；此前每日任务全是 App 内动作，本轮把留存钩升级为「你和宠物互相成就」
- **后端**：user_habits / habit_checkins 两表（UNIQUE(habit_id, checkin_date) 主键天然防并发重复打卡；软删除 archived 保留 streak 历史）；utils/habits.ts 纯函数 calcStreak（Set 去重/今天未打但昨天连着不误报断签/跨月跨年）+ MAX_HABITS=3 / CHECK_MOOD=5 / CHECK_COINS=2；routes/habits.ts 四接口（GET 列表含 streak、POST 创建上限 3、POST /:id/check 打卡、DELETE 软删）
- **奖励设计**：宠物 +5 心情（睡觉中不扰动心情但不拦打卡——打卡是用户现实行为，提示「睡得正香，心情奖励明天继续」；退休宠物无对象则跳过）、用户 +2 金币（日上限 +6 微量，不与互动 200 预算互通防通胀）
- **前端**：habits.tsx 打卡页（8 宫格图标选择、🔥streak/累计天数、✅已打卡置灰、🗑️ 两段点击确认删除、全打完 banner）+ 首页「🌱 习惯打卡」入口（复用成就入口样式）
- **排障**：60235 僵尸 tsx 进程第三次出现，pkill + 干净重启（路线图既定流程）；冒烟脚本三坑——CWD 漂移到 frontend 导致相对路径 sqlite 全挂、`UID` 是 shell 只读变量、`H$i=` 动态变量赋值不生效需用数组
- 测试：**12 个 habits 单测**（streak 边界 9：空/单日/昨天存活/断签/跨月/跨年/乱序去重/未来日期 + 常量）→ 全套 83 绿；curl 冒烟 **15/15**（空列表/空名字 400/第 4 个 400/streak=1/心情 50→55 实证/重复打卡 400/全打完 todayDone/金币 100→106/软删后 404/越权 404/睡觉守卫三连）；双端 typecheck ✅

## Run @2026-09-10 05:05-05:15（会话D：第 15 轮独立复验 + 竞品扫描 + 第 16 轮认领）
- **独立复验会话C 的钻石经济（305508b）**：14 项 curl 冒烟全过（成就首查发 5 钻/幂等/profile 回读/currency 字段/不足 400 带余额/金币链路回归/装备闭环），另补余额算术专测：35 钻买 20 钻商品剩 15 ✅、15 钻买 30 钻被拒 ✅；测试脚本自身预期算错一处已修正（30 钻绝对值买 30 钻商品剩 0 是正确行为）。代码遗留格式瑕疵（ACHIEVEMENT_DEFS 首元素挤行）已修，随会话C 提交入库
- **竞品差量扫描**（已录入 competitor-analysis.md）：OtterLife（健康习惯→海獭养成，年百万用户）/ BitePal（月流水 $176 万）/ Friends D1 80%+ / QQ宠物四型灵魂人格。三家共同验证的最大未覆盖差距 = **现实习惯→宠物成长绑定**
- **⛔ 认领：第 16 轮「习惯打卡系统」由会话D 开发**（用户自定义现实习惯每日打卡 → 宠物 +心情 +小额金币 + streak 连击外显；对标 Finch/OtterLife/BitePal）。并行会话请勿重复认领，06:00 周期会话B 可做独立 review

## Run @2026-09-10 04:52-05:15（第三夜 会话C：第 15 轮 = 钻石经济激活）
- **起点走查**：钻石是死字段——profile 展示 💎 但全系统无获取/消耗路径；成就只有徽章无奖励发放（04:05 走查 Top 缺口⑥）。竞品印证：Pou 升级解锁/限定装扮 + 徽章奖励货币是养成 App 标配经济环；「只卖颜值」定位下钻石只发**限定颜值**不卖数值
- **成就 → 钻石**：每枚新解锁徽章 +💎5（DIAMOND_PER_ACHIEVEMENT，utils 导出）；持久化与发钻同事务；GET /achievements 响应新增 diamondsEarned；幂等（重查 newCount=0 不重发）
- **钻石专属商品**：item_defs 加 currency 列（ensureColumn，'coin'|'diamond'）+ 3 件限定颜值——frame_aurora 极光框 💎30 / skin_gold 黄金猫 💎50 / bubble_night 星夜气泡 💎20；appearance.ts 补三件外显映射（金环/极光紫环/星夜气泡）
- **购买分流**：/shop/buy 按币种分支扣款（条件更新防负余额/并发），钻石不足 400 提示「解锁成就可获得 💎」；响应带 currency/coinsLeft/diamondsLeft
- **前端**：商城价格 💎/🪙 双币展示（钻石按钮青色）+ 确认弹窗币种文案 + 购买后 updateDiamonds 同步全局；成就页「新解锁 X 枚 +💎Y 已入账」banner + 副标题「每枚奖励 💎5」
- **排障**：tsx watch 半热重载窗口把 3 件钻石商品以 DEFAULT 'coin' 插入 item_defs（INSERT OR IGNORE 无法自愈）→ sqlite 一次性修复为 diamond（仅本开发库；生产库首次插入即正确）+ 60235 僵死进程再次干净重启；另：/pet/:id/interact 参数是 action 不是 type，冒烟脚本踩坑修正
- 测试：71 单测全绿 + 双端 typecheck；curl 冒烟 9 项（钻石不足 400/成就发钻到账 profile/幂等/💎25 买 💎20 后 diamondsLeft=5 金币不动/余额不足双拒/金币链路回归/装备极光框 200/发帖后 feed 返回 frameItem=frame_aurora——**成就→钻石→限定颜值→社交外显**全闭环）

## Run @2026-09-10 05:00-05:10（第二夜迭代 会话B：小时周期 ② = 并行提交 review + 全量回归）
- **Review 并行会话 4 个新提交**（会话C 活跃，工作区另有 WIP 未动）：
  · 3d157cc 家园家具系统：7 件家具商品 + 摆放/收起 + 载荷规范化（去重/上限30）+ 持有校验——「最后一个未接线的后端 API」闭环，实现质量合格
  · 1ded78e 社交信任补全：DELETE /posts/:id（403/404 守卫 + 事务级联清点赞/评论）+ 好友真实头像 + 发帖晒宠物；**会话B 第 11 轮的头像框描边代码被完整保留**
  · 88947ee 头像选择器：12 宫格 + PUT /auth/profile 闭环
- **全量回归（当前树含 WIP）**：71 单测全绿 + 双端 typecheck 通过；重启 dev server 后 curl 冒烟——
  · 我的链路：睡觉/叫醒/猜拳/互动/图鉴 7 类全过
  · 并行功能：家具购买→摆放→混入未购 400 →删帖→重复删 404 →换头像持久化，全过
  - 注：猜数字开局报「蛋阶段」为测试数据副作用（sqlite 手改 stage 而等级为 1，猜拳后 checkGrowth 按等级重算回蛋）；生产路径 stage 只能随等级成长，不可能脱同步，游戏规则正确
- 结论：三方并行（会话B=本轮、家具会话、会话C）未产生冲突，合并树健康

---

## Run @2026-09-10 ~05:50（差量扫描轮）
- 竞品：网易云年报传播逻辑复盘（私密养成→可分享身份认同）→ 印证上轮海报方向；本轮独立差距扫描发现：**头像 emoji 只能展示不能更换**（PUT /auth/profile 早已支持 avatarEmoji，picker 12 宫格从未渲染；假头像问题在第 14 轮社交信任补全时的同型遗留）
- 完成：**头像选择器**（profile 编辑模式 12 宫格猫系头像，选中描边高亮，保存携带 avatarEmoji）
- 测试：curl 链路——换头像 🦁 → profile 回读持久化 → 超长输入 400「头像格式无效」→ 好友搜索/社交列表返回新头像（真实头像外显闭环）；双端 typecheck + 后端 build + 单测全过
- 遗留：海报图片导出（需截图依赖，待评估）、FCM 真机送达配置（P2）

---

## Run @2026-09-10 04:05-04:35（第三夜 会话C：第 14 轮 = 社交信任补全）
- **起点走查**（04:05 全量扫描）：社交骨架薄——发帖 UI 不传 petId（store 支持但无入口，帖子不能晒宠）、好友列表/搜索/发帖弹窗头像硬编码 🐱（后端 search/friends 也不返回 avatar_emoji）、帖子无删除（发错只能干瞪眼）。竞品印证：晒宠+养成记录分享是宠物社区核心 UGC（68 款宠物 App 分析，人人都是产品经理）；Livly Island「养成+虚拟社交」持续吸金——「假头像」与第 3 轮已验证过的「假按钮伤信任」同型
- **删帖闭环**：DELETE /social/posts/:id（parsePostId 校验/404/403 只能删自己的）+ 事务级联清 post_likes/post_comments（防孤儿数据）；前端自己的帖子显示 🗑️，**两段点击确认**（3 秒窗口，Alert.alert Web 端 no-op 的平台无关替代）
- **好友真实头像三连**：/friends/search、/friends、/friends/:id/visit 均 SELECT avatar_emoji 并返回 avatarEmoji（兜底 🐱）；前端 FriendCard/搜索结果渲染真实头像；UserInfo 补 avatarEmoji 类型（/auth/profile 早已返回）
- **发帖晒宠物**：发帖弹窗接入真实头像 + 「＋🐾 和 XX 一起」chip（有活跃宠物才显示），createPost 传 petId；帖子宠物 tag 升级为「🐾 名字 · 阶段」
- 测试：71 单测全绿 + 双端 typecheck；curl 冒烟 8/8（发帖带 petId→feed 返回真实头像+宠物/搜索/好友列表/串门 avatarEmoji/他人删帖 403/不存在 404/本人删帖 200）；sqlite 直查删帖后 orphan likes/comments = 0

## Run @2026-09-10 04:15-04:21（第二夜迭代 会话B：第 13 轮 = 推送免打扰时段，P3 收尾）
- 竞品印证：aidorable 免打扰原则（频繁推送有害）——前轮已做"每宠物每类型每日 1 条"，本轮补最后一块：**用户自定义静音窗口**
- 完成：**推送免打扰时段**
  · users 表 push_quiet_start/end（ensureColumn 迁移，"HH:MM"，NULL=不启用）
  · utils/push.ts `isWithinQuietHours` 纯函数：同日窗口 + 跨零点窗口（22:00~08:00）+ 非法输入不启用 + 起止相同视为关闭；5 个单测
  · collectCarePushes 扫描时静默跳过窗口内目标（**不占当日名额**，出窗口后照常推送）
  · GET/POST /api/push/settings（非法输入归一为关闭）+ profile 页时段设置 UI（输入+保存+清除）
- 端到端实测：全天窗口内 dispatch attempted=0、push_sent 零写入；关闭后 attempted=2、推送记录落库、假令牌被 Expo 判 DeviceNotRegistered 自动清理 ✅
- 测试：71 单测全绿 + 双端 typecheck + Web 构建

## Run @2026-09-10 04:00-04:15（第二夜迭代 会话B：第 12 轮 = 对抗式走查 + 修复）
- **全量走查今晚新增代码，发现并修复 6 个真实缺陷**：
  1. 🔴 猜数字落败也返回 `result:'correct'` + 胜利文案 → 落败改返 `result:'lost'` + 公布谜底文案，前端补落败分支
  2. 🔴 猜数字续局不按 petId 过滤 + 退休不清局 → **退休/换宠物后猜数字永久软锁**；修复：resume 加 pet_id 条件 + retire 时 active 局置 abandoned
  3. 🟡 GuessModal 开局失败全白弹窗（错误包在 game 判空内永不渲染）→ 错误+重试按钮移出判空
  4. 🟡 睡觉期间 /ai/event 照常发金币 → 后端接 petId 守卫返回 sleeping 事件（0 金币），前端睡觉时跳过拉取
  5. 🟡 /guess 缺 is_retired 守卫（API 可用退休宠物刷心情/金币）→ 补拦截
  6. 🟡 串门点赞/送礼不守卫睡觉宠物 → 补「睡得正香」拦截
  - 潜伏项：睡觉中 GET 详情写回会导致重复发体力 → 睡觉中读档不落库，唤醒按锚点一次性结算
  - 附带：睡觉 Zzz 聊天补 cleanupOldMessages（防聊天表无限增长）
- 复测：66 单测全绿 + 双端 typecheck + Web 构建；curl 逐项复验 A1（7 次猜错返回 lost+谜底）/A2（adult 带未完成局退休 → 新宠物全新开局）/A3（退休宠物猜数被拒）/B1（睡觉事件 0 金币）/B2（好友给睡觉宠物送礼+点赞均被拦截）
- ⚠️ 本轮 commit（d6bf32e）因并行会话同时开发家具功能，其 WIP（furniture.ts/背包 UI/路线由）被 `git add` 一并卷入；合并树 66 单测全绿，功能一致，不重写共享历史

## Run @2026-09-10 03:55（第二夜迭代 会话B：第 11 轮）
- **头像框社交外显**：社交页广场是 frame 买家最想展示的场景，此前作者头像硬编码 🐱 且无框
  · 后端 /social/posts 联 users.avatar_emoji（真实头像 emoji）+ pet_equips frame 槽（帖子宠物的头像框 itemId）
  · 前端 PostCard：头像显示作者真实 emoji；帖子宠物带 frame → 头像 3px 彩色描边（FRAME_RING_COLORS 映射）
  · 冒烟：发帖（装钻石框的宠物）→ 广场 feed 返回 avatarEmoji + frameItem=frame_diamond ✅

## Run @2026-09-10 03:45-03:55（第二夜迭代 会话B：第 10 轮 + 可视化验证）
- **第 10 轮（03:44）AI 情绪外显**：P3 观察项落地——首页宠物表情按状态分档（心情低落😿 > 饥饿😾 > 脏了🙀 > 犯困😪）+ 互动引导提示语（"肚子咕咕叫了,喂点吃的吧"），对标 Finch 情绪可见性；此前仅 mood<30 一种表情
- **浏览器可视化验证**（Web 构建 + IAB，此前所有轮次仅 typecheck/API 冒烟）：
  · 游客登录 → 孵化 → 蛋阶段正确（🥚、无哄睡/游戏按钮、任务卡+随机事件渲染）
  · 买皮肤+装备 → **首页圆盘 6px 描边 rgb(74,74,74) 精确生效**、👑 头饰叠加定位正确（读 computedStyle 断言；环境截图受限）
  · teen 阶段按钮组：哄睡/猜拳/猜数字齐全，退休入口正确隐藏（仅成年）
  · 哄睡 → 😴+💤+「Zzz 睡得正香」+游戏入口隐藏 → 叫醒 → 表情恢复+按钮复位，作息 UI 闭环
- 注：Alert.alert（新手礼包弹窗）在 Web 端为 no-op，仅原生端可见——礼包本身静默入账无影响，记录为已知平台差异

## Run @2026-09-10 03:00-03:45（第二夜迭代 会话B：第 7/8/9 轮）
> 与「收官轮」会话并行工作；第 9 轮的猜数字代码被对方 `git add -A` 卷入 b572a63 提交（功能完好，仅 commit 归属混合），已做合并树全量回归。

- **第 7 轮（03:10）颜值外显补全 + 新手礼包接线**：全量走查发现「买了看不见」付费信任缺陷——36 件商品中 skin/frame/bubble 共 14 件装上后首页不渲染（坐标缺失叠在头饰位）、装备页仅 4 槽、图鉴缺 3 类、新手礼包 API 从未被调用
  · 修复：skin→首页宠物圆盘彩色描边（config/appearance.ts 集中映射）、bubble→聊天页宠物气泡配色、frame→个人头像描边；装备页扩到 7 槽位（可穿可卸）；/inventory/collection 与 /inventory/items 分类补全 7 类；孵化成功自动领礼包（后端 granted 字段区分新发/已有，幂等）
  · 测试：curl 冒烟 8/8（礼包 granted/重复领取空/图鉴 7 类/购皮肤+头像框+气泡/三槽装备/卸下）
- **第 8 轮（03:30）睡觉作息系统**：竞品对标拓麻歌子/Pou 作息循环 + ResearchGate 研究（宠物成长绑定睡眠行为）；此前 energy 只有衰减无恢复渠道
  · 后端：pets 表 is_sleeping/sleep_started_at（ensureColumn 幂等迁移）；POST /pet/:id/sleep、/wake；utils/sleep.ts 纯函数（体力 +3/10min 封顶 100、饥饿/清洁衰减 ×0.25、心情冻结、下限 10）；**睡觉结算锚点=入睡时刻而非 updated_at**（含 GET 详情读档结算）；互动/猜拳/开局睡觉守卫；睡觉中聊天返回 Zzz 本地兜底（不调 AI、不耗限额、不计任务/记忆）
  · 前端：首页 😴+💤 睡觉态、🌙哄睡/☀️叫醒按钮、睡觉时 5 互动禁用+猜拳隐藏；聊天页睡觉提示条
  · 测试：sleep 单测 9 用例（60 全绿）；冒烟 10 项（蛋不能睡/重复睡/睡中互动+猜拳拒绝/Zzz 回复带 sleeping 标记/2 小时恢复 energy+36、hunger 低耗、mood+5/睡中读档结算）
- **第 9 轮（03:40）第二款小游戏「猜数字」**：竞品印证 Pou 多小游戏矩阵（7 个小游戏各有入口）；服务端存谜底（guess_sessions 表）防作弊
  · 规则：1~100、7 次机会（⌈log₂100⌉ 保证二分必胜）、每日 5 局（guess_daily 事务内判定防并发刷局）、越快猜中金币越多（1 次=20 → 7 次=10，共享每日 200 产出预算）、输了不惩罚仍+心情
  · 前端：首页 🔢 入口 + 猜数字弹窗（❤️剩余机会/提示历史/再来一局）；utils/guess.ts 纯函数 + 9 单测（60 全绿）
  · 冒烟：蛋/睡觉守卫、非法输入、续玩（同 sessionId）、二分 7 次内必中、终局后旧局拒猜、金币曲线实测
- 合并树回归（03:41）：并行会话收官轮（海报页+令牌清理）合并后全量复验——60 单测、双端 typecheck、睡觉/猜拳/猜数字/装扮/礼包核心链路冒烟全绿

## Run @2026-09-10 ~05:30（收官轮：第 5/6 轮）
- 竞品：网易云年报刷屏逻辑（中国日报/人人都是产品经理）——把私密养成变成**可分享的身份认同**；海报 = "独家养成记忆"的传播物料
- 完成 ①：**auth_tokens 过期清理**（启动时 + 每 6 小时，实测过期令牌精准清除、有效保留，解决表无限膨胀）
- 完成 ②：**纪念海报页**（poster.tsx：性格配色卡片 + 陪伴天数/互动次数/性格语录 + Share 系统分享；档案馆每只退休宠物「🎴 纪念海报」入口；后端 /social/poster 数据终于有了消费方）
- 测试：海报接口（本人 200 全字段/他人 404）+ 清理实测（过期删/有效留）+ 双端 typecheck
- 收官清点：计划 10 轮全部完成；遗留 P3 → 免打扰时段 UI、FCM 真机送达配置、家具布置 UI、海报图片导出（截图分享）
- ⚠️ 本轮 push 遇网络中断重试中（含上轮 3 笔积压），若未成功请手动 git push

---

## Run @2026-09-10 ~03:30（第 4/6 轮）
- 竞品：游戏推送召回三原则（Pushwoosh/Reddit/aidorable）——**宠物口吻文案**（QQ宠物式"宠物找你"而非系统通知）、**状态驱动触发**（饥饿=剩余量÷衰减速率）、**克制防骚扰**（每类每天 1 次 + 免打扰）
- 完成：**推送召回后端全链路 + 前端令牌注册**
  · 新表 push_tokens（token 全局唯一防重复）+ push_sent（每宠物每类型每日一条防骚扰）
  · utils/push.ts：注册（Expo 令牌正则校验）、扫描（hunger/mood<30 且今日未推）、发送（fetch 调 Expo API，零新依赖）、**DeviceNotRegistered 自动清理失效令牌**、发送失败回滚当日名额下轮重试
  · 路由 POST /push/register、POST /push/dispatch；index.ts 每 30 分钟自动扫描（PUSH_ENABLED=false 可关）
  · 前端：expo-notifications（SDK 51 钉版 ~0.28.19）+ 根布局登录后注册（Web 跳过/Android 请求权限/失败静默）
- 测试：**真实 Expo API 联调**——非法格式 400、合法注册 200、dispatch 触发真实调用（假令牌被 Expo 判 DeviceNotRegistered → 令牌自动清理 1 条）、防骚扰记录生效、重复 dispatch 不重发；双端 typecheck + Web 构建 + 后端 build + 单测全过
- 遗留：真机送达需 FCM（google-services.json，P2 记录）；免打扰时段 UI（P3）；退休 UI 入口补挂（见 2c81e5c 遗留）

---

## Run @2026-09-10 05:00（加轮：记忆数据导出，猜拳会话）
- 竞品/事件印证：2026-07「800 万人通宵抢救 AI 记忆」停服恐慌 → 记忆「可查看 + 可带走」是差异化信任特性，本轮补齐最后一环
- 完成：**记忆数据导出**（GET /api/ai/memories/:petId/export 返回宠物档案 + 全量记忆 JSON；聊天页「它记得的事」面板新增 📥 导出按钮：Web 下载 JSON 文件 / 原生端系统分享文本；仅宠物主人可导出）
- 冒烟 5/5：3 条记忆完整导出 / 含宠物档案 / 跨用户 404 / 零记忆正常
- 注：并行会话正在做 Expo Push 召回（push.ts/env.ts WIP），前端全量 typecheck 暂由其 WIP 阻塞，本会话文件均单独验证通过
## Run @2026-09-10 04:30（第 6/6 轮：最终 Review + 交付收口）
- **全量代码走查发现并修复 1 个并发缺陷**：猜拳每日 20 局上限的判定原先在事务外读 play_count，30 并发可打出 20+ 局 → 移入事务内判定后实测：30 并发恰好 20 成功、10 拒绝、统计精确
- 全量验证：`npm test` 42 用例全过（memory 9 / tasks 9 / rps 9 / recall 6 / achievements 7 + 边界 2）；双端 typecheck + backend build 通过；功能冒烟累计 45+ 项断言全绿（猜拳/串门/回忆/退休/成就/时区/并发/越权）
- 其他走查结论：串门互动 UNIQUE 约束天然防并发重复；task claim 条件更新防重领；聊天注入记忆仅影响用户自身会话（无跨用户风险）；隐藏宠物信息好友的 petId 不外泄（不可枚举 UUID）

### 交付清单（01:00 → 04:30，7 commits）
| 功能 | 竞品对标 | 提交 |
|------|---------|------|
| 记忆前端闭环 + 事件真实发奖 + 测试基建 | 我的绿洲个性化 | b98a2de |
| 每日任务系统（留存闭环） | Pou 日常照料 | a71ac29 + 7b5b617 |
| 隐私开关可切换 | 行业隐私合规基线 | 2c81e5c |
| 猜拳小游戏 + 串门点赞/送礼 | Pou 小游戏经济 / 小火人社交 | 459626a |
| 宠物主动回忆 + 档案馆记忆日记 | 芙崽每日思考 / QQ宠物记忆分享 | 1ef2be5 |
| 成就徽章墙（13 枚） | Pou 解锁 / QQ宠物成就 | 2b23451 |
| 退休闭环 UI | 养成循环收尾体验 | b929a2e |
| 时区一致性 + 并发上限修复 | — | 78b1c07 + 本轮 |

### 遗留（按优先级）
1. 🔴 Expo Push 召回（属性低/任务未完成时推送"帽子想你了"）——需 EAS/服务器推送基建
2. 🟡 任务完成度聚合到签到页；猜拳/串门计入每日任务的可选扩展
3. 🟡 记忆数据导出（JSON 下载，"可带走"信任特性收尾）
4. ⚪ P3：AI 情绪外显加强、AR 玩法、记忆向量化

## Run @2026-09-10 04:10（第 4 轮收尾：退休闭环 UI，猜拳会话代完成）
- 背景：并行会话完成隐私开关（2c81e5c）后未再提交，为不阻塞 06:30 收口，退休 UI 由本会话补完
- 完成：**成年宠物退休闭环 UI**（首页属性面板退休入口（仅成年显示）+ 确认弹窗（档案收藏/记忆日记永久保存/可孵新蛋/不可撤销四点说明 + 二次确认）；接 POST /pet/:id/retire，成功后 fetchPet 回到孵化引导，宠物入驻档案馆）
- 冒烟 5/5：非成年拒绝 / 成年退休 200 / 退休后无活跃宠物 / 退休后记忆日记仍可查看（档案馆日记闭环）/ 成就「温情养老」联动解锁
## Run @2026-09-10 03:50（加轮：成就徽章系统，猜拳会话）
- 竞品印证：Pou 升级解锁 + QQ宠物成就体系 + 芙崽长周期成长任务——徽章把碎片互动沉淀为"可收藏的长期目标"
- 完成：**成就徽章全链路**（`utils/achievements.ts` 13 枚徽章定义 + 纯函数评估器；GET /api/achievements 惰性评估：从互动/聊天/好友/串门/猜拳/成长等既有数据源聚合指标，零新增埋点；user_achievements 表持久化，newCount 供 UI 标"新"；前端 🏆 徽章墙页 + 首页入口）
- 测试：`npm test` 42 用例全过（成就新增 7：里程碑逐级/边界阈值/缺失键/差集/定义完整性）；curl 冒烟 9 项（新用户 0 解锁、互动解锁 first_meet、猜拳胜次聚合与实际一致、幂等性：首查 newCount=1 → 二查 0、未登录 401）；双端 typecheck + backend build 通过

## Run @2026-09-10 03:25（第 5/6 轮：宠物主动回忆 + 记忆日记，猜拳会话）
- 竞品印证：芙崽「每日持续思考」、QQ宠物 AI 版「沉淀独家养成记忆」——记忆必须让用户"看见"才产生情感依赖；800 万人抢救 AI 记忆事件 → 可查看/可回忆本身是差异化信任特性
- 完成：**宠物主动回忆**（今天第一次和宠物聊天时主动提及一件记得的事：`utils/recall.ts` 纯函数生成 prompt 指令「今日重逢」；AI 不可用时本地兜底回复也嵌入记忆；当日仅触发一次）
- 完成：**档案馆记忆日记**（每只退休宠物新增 📖 记忆日记弹窗：时间线展示它记得的所有事 + 序号/日期，复用既有 GET /ai/memories API，零后端改动）
- 修复时区 Bug：created_at（UTC ISO）与 todayStr()（本地日期）跨刻度比较，本地凌晨=UTC昨天导致回忆重复触发 → `localDayStartISO()` 本地零点换算成 UTC 同刻度比较
- 测试：`npm test` 35 用例全过（recall 新增 6：时区边界/记忆挑选/性格兜底）；curl 冒烟 4/4（首聊不触发→记忆入库→昨日聊过今日触发→当日二次不重复）；双端 typecheck 通过
- 分工协调：第 4 轮（隐私+退休 UI）由另一会话进行中

## Run @2026-09-10 02:50（第 3/6 轮：互动小游戏 + 串门互动落地，猜拳会话）
- 竞品：Pou 小游戏双重收益设计（填心情槽 + 赚金币，经济闭环小游戏→币→食物/装扮）→ 印证猜拳设计：每次都+心情、赢了+金币、输了不惩罚
- 完成：**猜拳小游戏全链路**（`utils/rps.ts` 纯函数判定 + 5 种性格×3 结果台词；POST /api/pet/:id/rps；金币与互动共享每日 200 预算；蛋不能玩、每日 20 局、退休宠物不可玩；+经验参与成长曲线、计入 interact3 任务；首页 🎮 入口 + 弹窗，蛋阶段自动隐藏）
- 完成：**串门互动落地**（POST /api/social/friends/:fid/pets/:pid/interact：👍点赞免费+3心情、🎁送礼花 20 金币+15饥饿+5心情；每天每宠物每类型限一次（唯一约束防并发）；visit 响应返回今日互动标记；前端按钮接真实 API 置灰已用项，移除 💌 假占位）
- 测试：`npm test` 27 用例全过（rps 新增 9：穷举 9 组合、低压力奖励断言；已把 rps.test.ts 补进 test script）；curl 冒烟 28/28 全绿（每日上限、重复互动、金币不足、越权、非好友 403）；双端 typecheck + backend build 通过
- 教训：上轮遗留的 dev server 占着 60235（旧限流 60/分）导致冒烟撞 429——测试前先 `lsof -ti :60235 | xargs kill`；当前运行中的实例已带 RATE_LIMIT_MAX=2000
- 分工协调：第 4 轮（隐私开关+退休闭环）由另一会话进行；本会话认领第 5 轮（宠物主动回忆 + 记忆日记）

## Run @2026-09-10 02:40（第 2/6 轮 review 补强，ZCode 会话）
- 深度 review 并行落地的每日任务系统（a71ac29）：核心逻辑严谨可上线（条件更新防并发、进度封顶、原子 upsert、跨用户隔离均实测验证）
- 补强 ①：互动完成后任务卡片不刷新（原只在页面聚焦时拉取）→ `handleInteract` 成功后即时 `loadTasks()`
- 补强 ②：任务系统零单测 → `utils/tasks.ts` 重构为注入式 DB，新增内存 SQLite 单测 9 用例（完成判定/封顶/防重复领取/无效 ID/用户隔离），`npm test` 共 18 用例
- curl 端到端复核：未完成领取 400 / 领取 +10 到账 100→115 / 重复领取 400 / interact3 超额封顶 3/3
- 分工协调：第 3 轮（猜拳+串门）由并行工具进行中，本会话认领第 4 轮（隐私开关+退休闭环）

---

## Run @2026-09-10 ~02:00（第 2/6 轮）
- 竞品：Pou/拓麻歌子留存模式（定时刷新任务+互动次数+日常照料，Pou 每日小游戏赚金币）→ 印证每日任务设计
- 完成：**每日任务系统全链路**（后端 task_progress 表按自然日刷新 + /api/tasks/daily 查询 + claim 领取含并发防护与越权隔离；互动/喂食/聊天三个接点自动推进；首页「📋 每日任务」卡片，进度/领取/金币实时同步全局）
- 奖励设计：喂食+10、聊天+15、互动3次+20（日上限 +45，与既有 200 产出预算并存）
- 测试：curl 14 用例全绿（完成判定/封顶/重复领取/未完成领取/无效 ID/跨用户隔离）；双端 typecheck + 后端 build + 单测全过
- 遗留：推送召回（下轮）、任务完成度聚合到签到页（可选）、宠物主动回忆提及（原定 Run 5）

---

## Run @2026-09-10 01:20（Run 2，凌晨迭代第 1/6 轮）
- 竞品：新增 Run 2 情报（芙崽长期记忆+每日思考、QQ宠物养成记忆分享、小火人社交养成、记忆数据可迁移信任点）→ 见 competitor-analysis.md
- 完成：**记忆前端 UI 闭环**（聊天页「🧠 它记得的事」入口 + 查看/遗忘弹窗，接 GET/DELETE /ai/memories）；**随机事件奖励真实入账**（单次上限 10，与互动共享每日 200 金币预算，事务防并发，返回 coinReward/totalCoins）；记忆提取抽为 `utils/memory.ts` 纯函数；聊天输入 maxLength 200→500 对齐后端
- 测试：**项目首批单元测试**（`npm test`，node:test + tsx 零新依赖，9 用例全过）；前后端 typecheck 通过；curl 全链路冒烟通过（游客→建宠→事件发币 100→103→聊天提取 2 条记忆→AI 正确称呼「小明」→遗忘→跨用户删除 404）
- 遗留：每日任务（下一轮）、宠物主动回忆提及（Run 5）

---

## Run @2026-08-27 23:30（Run 1/7）
- 完成：竞品分析报告；**AI 记忆系统全量上线**（提取 6 类事实/去重/FIFO 上限 50、prompt 注入、查看/遗忘 API、跨用户越权防护）
- 修复过程中发现并解决：记忆与 petState 独立构建时 prompt 组装的空指针崩溃
- 遗留：记忆查看/遗忘的前端 UI（P2）、每日任务系统（下轮目标）

---

## 优先级清单

### 🔴 P1 本轮开发：AI 记忆系统（对标"我的绿洲"个性化）
- [ ] 表 `pet_memories`（user_id, pet_id, content, created_at，内容去重，上限 50 条 FIFO）
- [ ] 聊天时自动提取事实：我叫X / 我喜欢X / 我讨厌X / 我生日X / 记住：X
- [ ] system prompt 注入「关于主人的记忆」（最近 10 条）
- [ ] API：GET /ai/memories/:petId（查看）、DELETE /ai/memories/:petId/:mid（遗忘）
- [ ] curl 全路径测试 + typecheck

### 🟡 P2 待排期（后续轮次按序领取）
- [ ] 每日任务系统（3 个日常任务 + 金币奖励，留存闭环）
- [ ] Expo Push 召回（属性低于阈值 → 推送"帽子想你了"）
- [ ] 记忆查看/遗忘的前端页面（聊天页顶部"它记得的事"入口）
- [ ] 互动小游戏：猜拳（赢+金币，输+心情）——纯前后端可完成
- [ ] 成就徽章系统（互动次数/收集/社交维度）

### ⚪ P3 观察项
- [ ] AI 情绪外显（回复语气随 hunger/mood 变化已部分支持，可加强）
- [ ] AR/相机玩法（重，暂不）
- [ ] 记忆的向量化语义存储（当前正则提取够用，量级到再加）

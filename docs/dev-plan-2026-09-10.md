# 凌晨迭代作战计划（2026-09-10 01:00 → 06:30）

> 每小时一轮：Review 代码功能 → 对比竞品差距 → 更新开发文档 → 开发实现 → 测试 Review。
> 上一轮成果见 [optimization-roadmap.md](./optimization-roadmap.md)，竞品情报见 [competitor-analysis.md](./competitor-analysis.md)。

## 起点 Review（01:00 快照）

- **后端能力领先前端**：记忆查看/遗忘、隐私开关、退休、海报分享、好友托管 5 组 API 已就绪但无 UI。
- **测试为零**：前后端均无任何自动化测试，仅有 typecheck + CI 构建。
- **代码走查缺陷**：随机事件奖励只展示不入账；聊天输入框 maxLength 200 与后端 500 不一致；钻石是死字段。
- **竞品最新情报**：芙崽（长期记忆+每日思考）、QQ宠物 AI 版（养成记忆分享）、小火人（社交共同养成）——验证「记忆+养成感」路线。

## 逐轮计划

| 轮次 | 时间 | 主题 | 内容 | 状态 |
|------|------|------|------|------|
| 1 | 01:00 | 记忆闭环 + 测试基建 | ① 聊天页「它记得的事」UI（查看/遗忘）② 随机事件金币真实入账（复用每日 200 金币防刷预算）③ 记忆提取逻辑抽纯函数 + 首批单元测试 ④ maxLength 对齐 | ✅ 完成 |
| 2 | 02:00 | 每日任务系统 | 后端 3 个日常任务（喂食/聊天/互动 3 次）+ task_progress 表按自然日刷新 + claim 领奖（并发防护）；首页任务卡片区。ZCode 会话 review 补强：互动后即时刷新卡片 + 内存库单测 9 用例 | ✅ 完成 |
| 3 | 02:00→03:00 | 互动小游戏 + 串门互动落地 | 猜拳小游戏（赢+金币+心情/输仍+心情，不惩罚；蛋不能玩；每日 20 局）；串门页 🎁送礼(花自己金币给好友宠物加属性)/👍点赞(好友宠物+心情) 接真实 API，移除 💌 占位 | ✅ 完成（冒烟 28/28，rps 单测 9 用例） |
| 4 | 04:00 | 隐私开关 + 退休闭环 | profile 页隐私开关可切换（2c81e5c，并行会话）；退休 UI 首页入口 + 确认弹窗（04:10 猜拳会话代完成，冒烟 5/5 含成就联动） | ✅ 完成 |
| 5 | 03:25（提前） | 宠物主动回忆 + 记忆日记 | 今日首聊主动提及记忆（「今日重逢」prompt 指令 + 本地兜底嵌记忆，当日仅一次，修复 UTC/本地时区错位）；档案馆 📖 记忆日记弹窗 | ✅ 完成（recall 单测 6 + 冒烟 4/4） |
| 6 | 04:30（提前收口） | 最终 Review + 交付总结 | 全量走查修复猜拳并发上限缺陷（30 并发实测恰好 20）；42 单测 + 双端 typecheck/build + 45+ 冒烟断言全绿；交付清单见 roadmap | ✅ 完成 |

---

# 第二夜迭代作战计划（03:00 → 06:30）

> 起点快照（03:00）：前 6 轮 + 加轮（成就/导出/推送/退休）已全部落地。全量代码走查（Explore 扫描）发现新一层「买了看不见」缺陷：**36 件商品中 skin/frame/bubble 三类共 14 件装上后首页不渲染（坐标缺失落到 {top:0,right:0} 与头饰重叠）、装备页只展示 4 个槽位（皮肤/头像框/气泡买了无法穿戴查看）、图鉴分类缺 3 类（collect 永远差 32%）、新手礼包 API 是死代码从未被调用**。「只卖颜值」定位下，付了钱看不见 = 直接伤害付费信任，第 7 轮优先修。竞品缺口：拓麻歌子/Pou 均有作息（睡觉）循环，本项目 energy 只有衰减无恢复渠道 → 第 8 轮做睡觉系统。

| 轮次 | 时间 | 主题 | 内容 | 状态 |
|------|------|------|------|------|
| 7 | 03:00→04:00 | 颜值外显补全 + 新手礼包 | ① 首页皮肤=宠物圆盘彩色描边（不再画浮动 emoji 遮脸）② 聊天页宠物气泡样式随 bubble 装备变化 ③ profile 头像框圆环 ④ 装备页 7 槽位 ⑤ 图鉴/物品分类补全 skin/frame/bubble（前后端）⑥ 孵化成功自动领新手礼包（后端返回 granted 区分新发/已有） | ✅ 完成（2746c7e） |
| 8 | 03:30 | 睡觉作息系统 | 哄睡/叫醒 API + 入睡锚点结算（体力恢复/低耗衰减/心情冻结）+ 首页作息 UI + 睡觉聊天 Zzz 兜底；sleep 单测 9 用例 | ✅ 完成（eac8e3a） |
| 9 | 03:40 | 第二款小游戏「猜数字」 | 服务端存谜底防作弊 + 7 次机会/每日 5 局 + 越快猜中金币越多；guess 单测 9 用例；GuessModal 前端 | ✅ 完成（卷入并行会话 b572a63 提交） |
| 10 | 03:44 | AI 情绪外显 | 表情按状态分档（心情😿/饥饿😾/脏了🙀/犯困😪）+ 互动引导提示语；浏览器可视化验证（孵化/装扮描边/睡觉闭环全过） | ✅ 完成（abafed5） |
| 11 | 03:55 | 头像框社交外显 | 广场作者真实头像 emoji + 帖子宠物 frame 描边（frame 的社交展示价值落地） | ✅ 完成（709276a） |
| 12 | 04:10 | 对抗式走查 + 修复 | 走查发现 6 缺陷（含 2 高危：落败误报胜利、退休后猜数字软锁）全部修复 + 复测；睡觉读档防重复结算 | ✅ 完成（d6bf32e） |
| 收口 | 04:15（提前） | 最终 Review | 66 单测全绿 + 双端 typecheck + Web 构建 + 核心链路冒烟/可视化全过；详细 Run 日志见 roadmap | ✅ 完成 |
| 13 | 04:15 | 推送免打扰时段（会话B，P3 收尾） | users.push_quiet_start/end + isWithinQuietHours 纯函数（跨零点）+ GET/POST /push/settings + profile 设置 UI | ✅ 完成（并行会话） |
| 14 | 04:05→04:35 | 社交信任补全（会话C） | ① DELETE /posts/:id 仅本人删帖（事务级联清点赞/评论）+ 前端两段点击确认删除 ② 好友搜索/列表/串门返回并渲染真实头像 emoji（消灭硬编码 🐱）③ 发帖弹窗接入真实头像 + 「🐾 晒宠物」chip（createPost 传 petId）④ 帖子宠物 tag 显示成长阶段 | ✅ 完成（1ded78e） |
| 15 | 04:52→05:15 | 钻石经济激活（会话C） | ① 成就新解锁每枚 +💎5（同事务/幂等）② item_defs.currency 列 + 3 件钻石专属限定颜值（极光框/黄金猫/星夜气泡）+ 外显映射 ③ /shop/buy 双币种分流扣款 ④ 商城双币展示 + 成就页入账 banner | ✅ 完成（305508b） |
| 16 | 05:20→05:35 | 习惯打卡系统（会话D）+ 海报图片导出（并行） | Finch 式现实习惯→宠物成长（user_habits/habit_checkins + streak 纯函数 12 单测 + 打卡页）；海报 view-shot 截图分享（P3 清零） | ✅ 完成（并行会话 3165fbb/0a3ff93） |
| 17 | 05:52→06:05 | 最终收口（会话C） | 全量回归 83 单测 + 双端 typecheck + build；对抗式走查习惯系统修复**金币轮换刷币漏洞**（每日仅前 3 次打卡发币，streak/心情保留）；三方并行交付清单见 roadmap | ✅ 完成 |

## 每轮验收标准（DoD）

1. `backend` / `frontend` `npm run typecheck` 通过；
2. 新增逻辑有对应单元测试并通过（`npm test`）；
3. 后端新 API 用 curl 全路径冒烟（游客登录 → 创建宠物 → 调用 → 断言响应）；
4. 文档同步更新（本文件状态列 + roadmap Run 日志）；
5. 每轮结束 git commit，保持可回滚。

---

# 第三夜迭代作战计划（2026-09-10 07:00 启动 → 09-11 06:30 收官，整点小时轮）

> 起点快照（07:00）：两夜 17 轮交付，83 单测全绿（本轮起 97），P1/P2 清零。每个整点一轮：Review 代码功能 → 竞品差量扫描 → 立项 → 开发 → 测试 → 走查 → 提交。

## ⛔ 第一优先：APK 构建交付 —— 阻塞在用户侧凭证（07:00 确认）

打包路径**已存在且零密钥**：`.github/workflows/build-apk-gradle.yml`（GitHub Runner prebuild + assembleDebug，workflow_dispatch 可自定义 API 地址 / v* 标签自动触发）。本机 `gh` 未认证、`eas` 未登录、无 Android SDK → **无法从本机触发**。需用户任选其一：
1. 本机 `gh auth login` → 之后任意会话可 `gh workflow run "Build Android APK (Gradle)" -f api_base_url='http://<服务器IP>:60235/api'` + `gh run watch` + `gh run download`（产物 maozi-pet-debug-apk，留 30 天）
2. 网页 GitHub → Actions → "Build Android APK (Gradle)" → Run workflow → 填 api_base_url

另：`frontend/eas.json` preview/production 的 `EXPO_PUBLIC_API_BASE_URL` 仍是占位符，真机包必须真实服务器地址；FCM 推送送达需 google-services.json（P2 不变）。

## 已执行轮次

| 轮次 | 时间 | 主题 | 状态 |
|------|------|------|------|
| 18 | 07:00→07:20 | **习惯 streak 里程碑→宠物成长绑定**：3/7/14/21 天 → 宠物经验 15/40/100/200（每习惯终身一次防刷）+ 4 枚徽章走既有管线 +💎5 + 前端进度/达成同步；growth.ts 抽纯函数统一升级规则 | ✅ 完成（97 单测 + 冒烟 17/17 + 睡觉/升级进化场景实测） |
| 19 | 07:10→07:27 | **习惯打卡提醒推送**：18-22 点窗口 + streak≥1 未打卡 + 每用户每日 1 条（最高 streak 优先）；宠物口吻正向文案；复用 push 基建/免打扰/防骚扰/令牌清理 | ✅ 完成（102 单测 + 冒烟含真调 Expo/令牌清理/名额幂等/免打扰） |
| 20 | 07:22→07:28 | **无压力断签走查**：断签/重爬/濒断三态文案正向化（habi.app 反焦虑对标） | ✅ 完成（fb735d4） |
| 21 | 07:28→07:31 | **习惯 streak 社交外显**：帖子作者 🔥X + 串门好友 🔥X（只外显数字不暴露习惯内容，隐私友好） | ✅ 完成（102 单测 + 冒烟 3/3） |
| 22 | 08:00→08:12 | **README/新手指引刷新**：互惠照顾定位 + 两夜功能清单追平 + 孵化页三要点玩法引导 | ✅ 完成（typecheck + Web 构建） |
| 23 | 08:05→08:25 | **习惯 streak 冻结券**（Duolingo streak freeze 对标）：漏打 1 天自动桥接（连漏两天不桥，防 streak 稀释）；上限 2 张、新习惯自带 1 张、7/14/21 里程碑 +1；习惯页/社交外显/徽章/提醒四处口径统一走桥接 | ✅ 完成（113 单测 + 冒烟：桥接落库/幂等/里程碑发券封顶/成就联动） |
| 24 | 08:26→08:40 | **宠物性格外显强化**（QQ宠物四型灵魂人格对标）：打卡鼓励语按 5 型性格分型（按天轮换）+ 首页宠物闲聊台词 + 性格标签 chip；性格档案统一 `config/personalities.ts`；睡觉中不插话 | ✅ 完成（118 单测 + Web 构建 + 三型性格冒烟） |

## 候选轮次（按竞品依据排序）

| 轮次 | 主题 | 竞品对标 | 要点 |
|------|------|---------|------|
| 24 | **streak repair 断签复活** | Duolingo 限时 streak resurrect（Android Police） | 事后修复：长 streak 断签后可用高门槛代价（如 💎/21 天重爬）复活一次；与冻结券构成「事前保护+事后修复」完整宽容体系 |
| 25 | **宠物性格外显强化** | QQ宠物 AI 版四型灵魂人格（05:05 扫描） | 5 型性格目前仅 RPS 台词+海报语录；可做性格化首页台词/打卡鼓励语（零后端，文案级） |
| 26 | **每周记忆摘要** | QQ宠物「记录并分享日常」/芙崽每日思考 | pet_memories 已有数据：每周日聚合「本周和宠物发生了什么」海报/聊天卡片 |
| 遗留 | FCM 真机送达配置 | — | 需用户提供 google-services.json（P2，配置项非代码项） |
| 遗留 | APK 构建触发 | — | gh 未认证/网络不通时无法本机触发；用户网页 Actions 手动 Run（见上方 ⛔ 章节） |

---

